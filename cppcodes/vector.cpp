#include<iostream>
#include<vector>
using namespace std;
vector <int> piles[1000000]
v.begin     v.end
vector <int> A;
vector <int> A(10);//initialising the size to 10

10 vectors - A[10] where A1,A2,.......are vectors

algo
sorting:
sort(A,A+N);
sort(myvector.begin(),myvector.end());//similar to array, begin() and end() are self explainatory

cout << lower_bound (a,a+10,3)-a<<endl; //lower_bound >=x and upper_bound>x

pair: 2 elements(or data types)
pair < int , char > a; // a pair of int and char
.first    .second
vector<pair<int,int>> a; // vector of pairs
a.push_back(make_pair<3,a>);
a[0].first a[1].first
sortint 3rd arg.

set://not like 5th element of array
set.insert 
set of vectors
a.find
a.erase
lower and upper bound in set too
set <string> gogolWords;//every element of set is a string
gogolWords.count(word);

map:
map<key,val>//key can be data type eg:m["karan"]=1 ,m[key]=val
m.count(key)==1 if key is present
m[" "]++; //after how many words a word occur in a list
int main()
{
    map < string, int > words;
    words["key"]=1;
    cout<<words["key"]<<endl;
    return 0;
}







